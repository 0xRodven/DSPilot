[project]
name = "dspilot-meta-harness"
version = "0.1.0"
description = "Meta-harness search loop for DSPilot Brain (Telegram agent)"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv==1.0.*",
]

[tool.uv]
package = false
