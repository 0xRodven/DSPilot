# Règles DSPilot — Always (toutes les tâches)

- `convex/_generated/` : jamais modifier (auto-généré)
- `mock-data.ts` : jamais importer (legacy, à supprimer)
- TypeScript strict : jamais `any`, utiliser `unknown`
- Confiance < 95% → poser des questions avant de coder
- Tests avant de déclarer terminé
